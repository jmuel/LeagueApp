var React = require('react'),
    PlayerHistoryForm = require('./PlayerHistoryForm');

module.exports = React.createClass({
    render: function() {
        return (
            <PlayerHistoryForm />
        );
    }
});